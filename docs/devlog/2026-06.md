# 2026-06 Devlog

## 2026-06-06

### Issue / PR

- Issue: 없음
- Branch: `develop`
- PR: 없음
- Commit: `0ee94ce` docs, feat: AI 지침 보강 및 문서 폴더 구조 변경

### 오늘의 작업

- AI 작업 지침과 문서 경로 기준을 현재 프로젝트 구조에 맞게 정리했다.
- `PostDetailPage`, `PostListPage`, `PostCreatePage`에서 API 조회 상태를 `isLoading`, `error` 상태로 분리해 화면을 제어하도록 수정했다.
- 게시글 상세/목록/작성 화면에서 조회 중 로딩 화면과 조회 실패 오류 화면을 표시하도록 개선했다.
- 브라우저에서 관련 화면이 정상 동작하는지 수동 확인했다.

### 변경 내용

- `PostDetailPage`
  - `isLoading`, `error` 상태 추가
  - 게시글 상세 조회 완료 후 로딩 상태 종료
  - 조회 중 `로딩 중...` 화면 표시
  - 조회 실패 시 오류 화면 표시

- `PostListPage`
  - `isLoading` 상태 추가
  - 게시글 목록 조회 중 로딩 화면 표시
  - 조회 실패 시 에러 화면으로 분기

- `PostCreatePage`
  - `isLoading` 상태 추가
  - 카테고리 목록 조회 중 로딩 화면 표시
  - 카테고리 목록 조회 실패 시 에러 화면 표시

- 문서
  - `.agents/skills/issue-driven-dev/SKILL.md`의 문서 경로 기준 수정
  - `docs/prompts/quick.md`의 PR/리뷰 지시문 일부 정리
  - `docs/review-checklist.md`의 문서 경로 기준 수정

### 수동 확인 결과

- 브라우저에서 게시글 관련 화면의 동작을 수동 확인했다.
- 확인한 항목:
  - [x] `PostDetailPage` 로딩/에러 상태 분기 확인
  - [x] `PostListPage` 로딩/에러 상태 분기 확인
  - [x] `PostCreatePage` 로딩/에러 상태 분기 확인
  - [x] 화면 렌더링 중 불필요한 오류가 발생하지 않는지 확인

### 문제 / 해결

- 문제:
  - API 조회가 끝나기 전 화면 렌더링과 조회 실패 상황을 명확히 구분하지 못했다.

- 해결:
  - 각 페이지에 `isLoading`, `error` 상태를 추가해 로딩 중 화면과 오류 화면을 분리했다.
  - API 요청 종료 시 `finally`에서 로딩 상태를 종료하도록 처리했다.

### AI 활용 기록

- Builder:
  - 게시글 상세/목록/작성 페이지의 로딩 및 에러 화면 처리 구현
  - AI 작업 지침과 리뷰 문서의 경로 기준 정리

- ChatGPT:
  - 방금 커밋 diff 기준으로 devlog 초안 작성 보조
  - 실제 연결 Issue/PR이 없는 작업으로 기록 범위 재정리
  - 브라우저 수동 확인 결과를 devlog에 반영

### 다음 작업 후보

- `npm run build` 실행으로 빌드 검증
- 상세/목록/작성 페이지의 에러 메시지 표현 방식 통일 검토
- 현재 문서 구조 기준으로 남아 있는 오래된 경로 참조 추가 확인

---

## 2026-06-07

### Issue / PR

- Issue: #3 `[Feature] auth: 회원가입 페이지`
- PR: #6 `feature/issue-3-signup-page` -> `develop`
- Branch: `feature/issue-3-signup-page`

### 오늘의 작업

- Issue #3 기준으로 회원가입 페이지 작업 범위를 `docs/current-task.md`에 정리했다.
- 회원가입 요구사항을 `docs/requirements.md`에 반영했다.
- 회원가입 API 사용 기준을 `docs/api-usage.md`에 보강했다.
- 회원가입 페이지에서 `POST /api/auth/signup` API를 호출하도록 연결했다.
- PR #6을 `docs/review-checklist.md` 기준으로 리뷰하고 GitHub PR에 리뷰 코멘트를 남겼다.

### 변경 내용

- 문서
  - `docs/current-task.md`
    - Issue #3 기준 작업 범위로 갱신
    - 성공 시 `/posts` 이동, 실패 시 `"회원가입 실패"` alert 표시 기준 명시
  - `docs/requirements.md`
    - 회원가입 validation, loading/error 상태, 성공/실패 흐름 보강
  - `docs/api-usage.md`
    - `POST /api/auth/signup` 요청 데이터, validation, 실패 처리 기준 보강

- 구현
  - `src/api/authApi.js`
    - `signup(signupData)` API 함수 추가
  - `src/SignupPage.jsx`
    - 이메일, 비밀번호, 닉네임 입력 상태 관리
    - 회원가입 제출 시 API 요청
    - 성공 시 `/posts` 이동
    - 실패 시 `error` 상태 설정 및 `"회원가입 실패"` alert 표시
    - 요청 중 `isLoading` 상태로 버튼 비활성화
  - `src/SignupPage.css`
    - 회원가입 form, input, error, button 스타일 보강

### 수동 확인 결과

- 이번 기록 작성 시점에는 브라우저 수동 확인 결과가 아직 기록되지 않았다.
- PR 리뷰 기준으로 코드 흐름과 diff를 확인했다.
- 확인한 항목:
  - [x] `/signup` route 연결 확인
  - [x] API 호출 경로가 `/api/auth/signup`으로 조합되는지 확인
  - [x] 성공 시 `/posts` 이동 코드 확인
  - [x] 실패 시 `"회원가입 실패"` alert 표시 코드 확인
  - [x] POST 요청이 `useEffect`에서 자동 실행되지 않는지 확인
  - [ ] 브라우저 정상 케이스 확인
  - [ ] 브라우저 실패 케이스 확인
  - [ ] Network 탭 Request Payload / Response 확인
  - [ ] Console 에러 확인
  - [ ] `npm run build` 확인

### 문제 / 해결

- 문제:
  - PR 리뷰 중 `docs/current-task.md`와 `docs/requirements.md`가 요구한 "Form 컴포넌트와 Page 제출 처리 양쪽 검증" 중 Form 레벨 검증이 부족한 것으로 확인했다.

- 해결:
  - PR #6에 리뷰 코멘트로 반드시 수정 항목을 남겼다.
  - `SignupPage` input에 `required`, `minLength`, `maxLength` 등 Form 레벨 제약 추가가 필요하다고 안내했다.

### AI 활용 기록

- Architect:
  - Issue #3 분석
  - `requirements.md`, `api-usage.md`, `current-task.md` 반영 초안 작성
  - 승인된 기준에 따라 문서 반영
  - `docs/review-checklist.md` 기준 PR #6 리뷰
  - GitHub PR에 리뷰 코멘트 등록

- Builder:
  - 회원가입 페이지 API 연결
  - 회원가입 상태 처리 및 스타일 보강

### 다음 작업 후보

- PR 리뷰 필수 수정 항목 반영
  - Form 레벨 검증 속성 추가
  - 이메일 검증을 trim 기준으로 통일
  - label/input 연결 개선 검토
- `npm run build` 실행
- 브라우저에서 회원가입 정상/실패 케이스 수동 확인
- Network 탭에서 `POST /api/auth/signup` Request Payload와 Response 확인
- Console에 불필요한 에러가 없는지 확인
